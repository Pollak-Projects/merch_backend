model Order {
  id        Int         @id @default(autoincrement())
  createdAt DateTime    @default(now())
  updatedAt DateTime    @updatedAt
  status    String
  total     Float
  delivery  Float
  items     OrderItem[]
  userId    String
}
